const auth = require('../auth')
const Comment = require('../models/comment-model');

createComment = async (req, res) => {
    
}

replyComment = async function(listId) {
    
}

module.exports = {
    createComment,
    replyComment
}